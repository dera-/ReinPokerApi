import ControllerBase from './ControllerBase.js';
import

export default PlayerController extends ControllerBase {
  async get(req, res) {
    const callback = (res, req) => {

    }
    await done(req, res, callback);
  }

  async post() {
    const callback = (res, req) => {

    }
    await done(req, res, callback);
  }
}
